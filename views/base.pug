doctype html
html 
    head 
        block head 
            meta(charset='utf-8')
            meta(name='viewport', content='width=device-width, initial-scale=1.0')
            //- meta(http-equiv="Content-Security-Policy" content="script-src 'self'  https://api.mapbox.com; https://js.stripe.com")
            meta(http-equiv="Content-Security-Policy" content="script-src 'self' 'unsafe-inline' 'unsafe-eval' https://js.stripe.com/v3 https://js.stripe.com  "  )
                //-    as we have use static middleware in app.js as public as root folder
            link( href="https://fonts.googleapis.com/css?family=Lato:300,300i,700", rel="stylesheet")
            link(rel='stylesheet', href='/css/style.css') 
            link(rel='shortcut icon', href='/img/favicon.png', type='image/x-icon')
            title Natours | #{title}
            //-  For booking alert
    body(data-alert=`${alert ?  alert : ''}`)
        //-Header
        include _header    

        //-Main
        block content       

        //- footer.footer
        include _footer
        
        //- script(src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js")
        //- script(type ="module" src='/js/login.js')
        //- script(src='/js/mapbox.js')
        
        script( src='https://js.stripe.com/v3')
        script( src='/js/bundle.js')